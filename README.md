# Ignore

This repo is an experiment to see if I can i) install an opencode plugin from a nested directory (located at plugins/opencode/) and ii) have that plugin pull in skills defined in a top-level skills/.
